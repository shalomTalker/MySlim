{% include 'home.twig'%}
{% set page_title = 'Show student' %}
{% block content %} 


				<div id="show" class="panel panel-default">
					<div class="panel-heading">
						<h4>Details of {{ student.name }}</h4>
					</div>
					<div class="panel-body">
							<div>
								<h5><strong>Name:</strong></h5>
								<span id="student_name">{{ student.name }}</span>
							</div>
							<div>
								<h5><strong>Phone:</strong></h5>
								<span id="student_phone">{{ student.phone }}</span>
							</div>						
							<div>
								<h5><strong>E-mail:</strong></h5>
								<span id="student_email">{{ student.email }}</span>
							</div>
							<div>
								<h5><strong>Created at:</strong></h5>
								<span id="student_created_at">{{ student.created_at }}</span>
							</div>
							<div>
								<h5><strong>Updated at:</strong></h5>
								<span id="student_updated_at">{{ student.updated_at }}</span>
							</div>
					</div>
				</div>
			</div>
		</main>
	</div>


{% endblock %}
