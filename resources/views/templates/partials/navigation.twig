
.<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
</head>
<body>
<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header row">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a href="{{ path_for('home') }}">
                <img src="{{ base_url() }}/images/logo.png" alt="logo" class="pull-left col-sm-1" id="logo-img">
            </a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
            {% if auth.check %}
                <li><a href="{{ path_for('home') }}">School</a></li>
            {% endif %}
            {% if auth.role > 1 %}
                <li><a href="{{ path_for('admin') }}">Administration</a></li>
            {% endif %}

            </ul>
            <ul class="nav navbar-nav navbar-right align-middle">
                {% if auth.check %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
                           aria-haspopup="true" aria-expanded="false">
                           {{ auth.user.name }} , {{ auth.user.role }}<span class="caret"></span>
                       </a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path_for('auth.password.change') }}">Change password</a></li>
                            <li><a href="{{ path_for('auth.signout') }}">Sign out</a></li>
                        </ul>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>﻿
    
</body>
</html>

